{
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "extensions": [
        "streetsidesoftware.code-spell-checker"
    ],
    "features": {
    },
    "runArgs": [
        "--env-file",".devcontainer/devcontainer.env"
    ],
    "remoteEnv": {
    },
    "onCreateCommand": [
    ],
    "postCreateCommand": {
     "SDP build": "make 2>&1 | tee /tmp/make_build.log",
     "SDP install": "make install SHELL='/bin/bash -x' 2>&1 | tee /tmp/make_install.log",
     "EXTENSION install": "while [ ! -f ./lsp/out/bin/dse.vsix ]; do sleep 2; done && code --install-extension ./lsp/out/bin/dse.vsix --force"
     },
    "portsAttributes": {
        "2159": {
            "label": "gdb"
        },
        "6379": {
            "label": "redis"
        }
    }
}
